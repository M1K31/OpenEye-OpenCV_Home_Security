# OpenEye — Raspberry Pi Requirements
# Use this instead of requirements.txt on Raspberry Pi (ARM Linux).
#
# Disabled features on Pi:
#   - Face recognition (dlib takes 2+ hours to compile, may fail on Pi 3)
#   - YOLO object detection (torch/ultralytics too heavy, ~2GB)
#   - Two-way audio (aiortc/pyaudio compilation issues on ARM)
#   - Cloud storage (heavy SDKs not needed for local Pi deployment)
#   - Firebase push notifications
#
# Retained features:
#   - Motion detection (OpenCV headless)
#   - Email/SMS notifications
#   - Smart home integrations (MQTT, HomeKit)
#   - Database (SQLite/PostgreSQL)
#   - WebSocket real-time updates
#   - Basic security (JWT, 2FA)

# Build tools
setuptools>=68.0,<82

# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Authentication & Security
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Environment variables
python-dotenv>=1.0.0

# Computer Vision — headless (no GUI needed on Pi)
opencv-python-headless>=4.8.1.78,<4.11
numpy>=1.24.0,<2.0.0
pillow>=10.1.0
scikit-learn>=1.3.0

# OPTIONAL: Face recognition — disabled on Pi (2hr+ dlib compile)
# dlib>=19.24.0
# face_recognition>=1.3.0

# Email notifications
aiosmtplib>=2.0.0
email-validator>=2.1.0

# SMS notifications
twilio>=8.10.0

# OPTIONAL: Firebase push notifications — heavy SDK
# firebase-admin>=6.3.0

# Async HTTP for webhooks
aiohttp>=3.9.0
httpx>=0.27.0

# Utilities
python-dateutil>=2.8.2
netifaces>=0.11.0
psutil>=5.9.0

# PTZ Camera Control
onvif-zeep>=0.2.12

# Smart Home Integrations
paho-mqtt>=1.6.1
HAP-python[QRCode]>=4.9.0
google-auth>=2.25.0
google-auth-oauthlib>=1.1.0

# OPTIONAL: Google Cloud events — heavy SDK
# google-cloud-pubsub>=2.18.0

# OPTIONAL: Cloud Storage — not needed for local Pi deployment
# boto3>=1.34.0
# google-cloud-storage>=2.14.0
# azure-storage-blob>=12.19.0

# Mobile Backend Support
websockets>=12.0

# OPTIONAL: Two-Way Audio — compilation issues on ARM
# pyaudio>=0.2.13
# aiortc>=1.6.0
# av>=11.0.0

# OPTIONAL: YOLO Object Detection — torch too heavy for Pi
# ultralytics>=8.0.0
# torch>=2.0.0
# torchvision>=0.15.0
# scipy>=1.10.0

# PostgreSQL
psycopg2-binary>=2.9.9

# Redis
redis>=5.0.1
hiredis>=2.2.3

# Additional Security
cryptography>=41.0.7
pyjwt>=2.8.0
pyotp>=2.9.0
qrcode[pil]>=7.4.2
