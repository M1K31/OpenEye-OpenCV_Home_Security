# CI/CD Testing Requirements
# Minimal dependencies for running tests without heavy packages

# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Authentication & Security
passlib[bcrypt]>=1.7.4
# python-jose was removed with the move to PyJWT. It is not listed here.
# It pulls in ecdsa, which carries CVE-2024-23342 (Minerva timing attack)
# and will not be fixed — pure-Python scalar multiplication is not
# constant-time, and the maintainers have said so. Nothing imports either.
#
# This file is standalone — it does not read requirements.txt — so the
# replacement has to be named here too, or nothing here can sign a token.
pyjwt>=2.8.0,<3.0  # JSON Web Token handling
bcrypt>=4.0.0

# Environment variables
python-dotenv>=1.0.0

# Computer Vision & Face Recognition
opencv-contrib-python>=4.8.1.78,<4.11  # Constrained for numpy<2 compatibility
numpy>=1.24.0,<2.0.0  # Constrained to <2 for dlib compatibility
face_recognition>=1.3.0
pillow>=10.1.0
scikit-learn>=1.3.0
dlib>=19.24.0

# Email notifications
aiosmtplib>=2.0.0
email-validator>=2.1.0

# Async HTTP for webhooks
aiohttp>=3.9.0

# Utilities
python-dateutil>=2.8.2
netifaces>=0.11.0
psutil>=5.9.0

# PTZ Camera Control
onvif-zeep>=0.2.12

# Two-Factor Authentication
pyotp>=2.9.0
# Must match requirements.txt: backend/core/two_factor_auth.py does `import qrcode`
# and uses qrcode.QRCode / qrcode.constants. pyqrcode is a DIFFERENT project that
# provides the `pyqrcode` module, so pinning it here left the import unsatisfied and
# every CI test run died in conftest before collecting a single test.
qrcode[pil]>=7.4.2

# Encryption
cryptography>=41.0.0

# WebSockets
websockets>=12.0

# Testing dependencies
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
# Fails a hung test instead of letting it block the runner until the
# 6-hour job limit (the suite has tests that can block on I/O).
pytest-timeout>=2.3.0
httpx>=0.27.0

# Cloud storage SDKs. tests/services/test_storage_service.py has 25 tests that
# patch boto3.client, google.cloud.storage.Client and azure BlobServiceClient;
# unittest.mock.patch imports its target, so without these installed the whole
# file fails or errors rather than testing anything. Together they cost ~42 MB
# (botocore is most of it) against torch's ~339 MB, so they do not reintroduce
# the disk pressure that motivated this trimmed-down file — and they buy back
# real coverage of the code path that moves user footage off the device.
boto3>=1.34.0
google-cloud-storage>=2.14.0
azure-storage-blob>=12.19.0

# Notification templating. alert_notification_system imports jinja2 directly; it
# used to arrive only as a transitive dependency of torch, which this file
# excludes, so the notification tests failed here while passing locally. ~1 MB.
jinja2>=3.1.0

# NOTE: Heavy optional dependencies still excluded for CI:
# - torch, torchvision, ultralytics (YOLO object detection)
# - firebase-admin (push notifications)
# - twilio (SMS)
# - paho-mqtt, HAP-python (smart home)
# - sounddevice (two-way audio)
#
# Also absent by design: ecosystem_client / ecosystem_auth / ecosystem_ai, which
# ship with the sibling appEcosystem project and are not published to PyPI. Tests
# that need them skip themselves — see tests/test_comprehensive_ecosystem.py.





